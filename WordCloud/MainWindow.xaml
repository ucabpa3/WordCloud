<Controls:MetroWindow x:Name="WordCloud" x:Class="WordCloud.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WordCloud"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="WorldCloud"   WindowStyle="ThreeDBorderWindow" ResizeMode="CanMinimize" WindowState="Maximized" >
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Colours.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/Blue.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Controls:MetroWindow.DataContext>
        <local:ProjectViewModel/>
    </Controls:MetroWindow.DataContext>

    <Grid HorizontalAlignment="Stretch" >
        <Grid.Resources>

        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition ></ColumnDefinition>
            <ColumnDefinition ></ColumnDefinition>
            <ColumnDefinition ></ColumnDefinition>
        </Grid.ColumnDefinitions>-->

        <Grid Grid.Row="0" Margin="3,3,0,0">
            <Grid.RowDefinitions>
                <RowDefinition ></RowDefinition>
                <RowDefinition ></RowDefinition>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="3,3,3,3"  Height="50" VerticalAlignment="Top">
                <Label Grid.Column="0" Content="Path:" Margin="0,3,0,15" />
                <Label Content="{Binding FullPath}" Margin="32,8,0,10" />
                <Button Width="88" HorizontalAlignment="Left" Content="Browse" Command="{Binding  GetPath}" Height="32" VerticalAlignment="Top" RenderTransformOrigin="-0.045,0.438" Margin="148,0,0,0"/>
            </Grid>
            <Grid Grid.Row="1" Margin="3,3"  VerticalAlignment="Bottom">
                <Button Width="88" Content="Generate" Command="{Binding  StartWordCloud}" Height="32" VerticalAlignment="Top" RenderTransformOrigin="-0.045,0.438"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="1" Margin="10" x:Name="CanvasContainer"  ShowGridLines="True" HorizontalAlignment="Stretch" >
            <ItemsControl ItemsSource="{Binding Path=Elements}" >
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path=Content}"  FontFamily="Verdana" Opacity="{Binding Path=Opacity}"  FontSize="{Binding Path=FontSize}"  >

                            <TextBlock.Foreground>
                                <SolidColorBrush Color="{Binding Path=Color}"></SolidColorBrush>
                            </TextBlock.Foreground>

                            <TextBlock.InputBindings>
                                <MouseBinding Command="{Binding DataContext.TextBlockClick,RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type TextBlock}}}" MouseAction="LeftClick" />
                            </TextBlock.InputBindings>
                        </TextBlock>

                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas ></Canvas>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter" >
                        <Setter Property="Canvas.Left" Value="{Binding Path=PosX}" />
                        <Setter Property="Canvas.Top" Value="{Binding Path=PosY}" />
                    </Style>

                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Grid>

    </Grid>
</Controls:MetroWindow>
