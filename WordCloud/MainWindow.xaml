<Controls:MetroWindow x:Name="WordCloud" x:Class="WordCloud.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WordCloud"  
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="WordCloud" WindowStyle="ThreeDBorderWindow" ResizeMode="CanMinimize" WindowState="Maximized" >
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Colours.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.AnimatedSingleRowTabControl.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/Blue.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Controls:MetroWindow.DataContext>
        <local:ProjectViewModel/>
    </Controls:MetroWindow.DataContext>
    <Controls:MetroContentControl>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Menu Grid.Row="0">
                <MenuItem Header="_File">
                    <MenuItem Header="_Open">
                        <MenuItem Header="_Open File" Command="{Binding  GetPath}" />
                    </MenuItem>
                    <MenuItem Header="_Close" />
                </MenuItem>
                <MenuItem Header="Operations">
                    <MenuItem Header="Parse" />
                    <MenuItem Header="Generate" Command="{Binding  StartWordCloud}" />
                </MenuItem>
            </Menu>
            <TabControl Grid.Row="1">
                <TabItem x:Name="CloudTab" Header="Cloud">   
                    <Grid Margin="10" x:Name="CanvasContainer" HorizontalAlignment="Stretch" >
                        <Controls:ProgressRing x:Name="WordCloudLoading" IsActive="False" />
                        <Border BorderBrush="LightGray"  BorderThickness="1" CornerRadius="5">
                            <ItemsControl ItemsSource="{Binding Path=Elements}" Margin="-1,-1,1,-1" Height="402" VerticalAlignment="Top" >
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>

                                        <TextBlock Text="{Binding Path=Content}"  FontFamily="Segoe WP" Opacity="{Binding Path=Opacity}"  FontSize="{Binding Path=FontSize}"  >

                                            <TextBlock.Effect>
                                                <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.3" RenderingBias="Performance" />
                                            </TextBlock.Effect>
                                            <TextBlock.Foreground>
                                                <SolidColorBrush Color="{Binding Path=Color}"></SolidColorBrush>
                                            </TextBlock.Foreground>

                                            <TextBlock.InputBindings>
                                                <MouseBinding Command="{Binding DataContext.TextBlockClick,RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type TextBlock}}}" MouseAction="LeftClick" />
                                            </TextBlock.InputBindings>
                                        </TextBlock>

                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                                
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas ></Canvas>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter" >
                                        <Setter Property="Canvas.Left" Value="{Binding Path=PosX}" />
                                        <Setter Property="Canvas.Top" Value="{Binding Path=PosY}" />
                                    </Style>                
                                </ItemsControl.ItemContainerStyle> 
                            </ItemsControl>
                        </Border>
                    </Grid>
                </TabItem>
                <TabItem x:Name="GraphTab" Header="Graph">
                    <Grid Margin="10" x:Name="CanvasGraph" HorizontalAlignment="Stretch" >
                        <Border BorderBrush="LightGray"  BorderThickness="1" CornerRadius="5">
                      
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>

    </Controls:MetroContentControl>
</Controls:MetroWindow>
